name: journal docker CI

on:
  push:
    branches: [ "main" ]

jobs:
  build-image:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag journal:$(date +%s)